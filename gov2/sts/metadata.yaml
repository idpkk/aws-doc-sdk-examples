files:
  - description: This example gets temporary security credentials to access resources.
    path: AssumeRole/AssumeRolev2.go
    services:
      - service: sts
        actions:
          - AssumeRole
  - description: test
    path: AssumeRole/AssumeRolev2_test.go
    services:
      - service: sts
        actions:
          - test