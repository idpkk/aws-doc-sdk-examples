files:
  - description: This code example gets temporary security credentials to access resources using version 2 of the AWS SDK for Go.
    path: AssumeRole/AssumeRolev2.go
    services:
      - service: sts
        actions:
          - AssumeRole
  - description: test
    path: AssumeRole/AssumeRolev2_test.go
    services:
      - service: sts
        actions:
          - test